# Netlify — static export of the Sereno Design System showcase.
# `next build` with `output: "export"` writes the site to `out/`.
[build]
  command = "npm run build"
  publish = "out"

[build.environment]
  NODE_VERSION = "20"
